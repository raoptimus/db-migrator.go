CREATE TABLE test (
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    top INT,
    title VARCHAR(255),
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX USING BTREE (top)
)
ENGINE = InnoDB;
